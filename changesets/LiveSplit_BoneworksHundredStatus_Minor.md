- Add a saved setting, enabled by default, to display level resets instead of item drop tries, with attempts and drop probabilities shown alongside the reset count. Preserve explicitly disabled settings in existing layouts. Requires the updated SpeedrunTools mod.
- Show reset counts starting at zero, always in the normal text color. Apply luck colors only to item names: white while pending at zero resets, gold when achieved at zero resets, and green at one reset fading to red at ten or more, whether pending or achieved.
- Restore status icons before each item in both display modes. Crosses are always white and ticks always green, independent of the layout text and item name colors.
- Floor the displayed overall RNG percentile and per-item total percentages instead of rounding to the nearest integer so that extremely unlucky percentages do not show as 100%.
- Replace the simulated overall RNG lookup table with a deterministic probability-weighted percentile. Rank runs using their item attempt counts, give half weight to tied outcomes, and keep scores between 0% and 100% with expected luck at 50%. Cache completed-run results to avoid recalculating during unrelated updates.
